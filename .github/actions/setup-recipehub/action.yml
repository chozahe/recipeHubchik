name: Setup RecipeHub
description: Install PHP, Composer dependencies, Node.js and frontend dependencies for RecipeHub.

runs:
  using: composite
  steps:
    - name: Setup PHP
      uses: shivammathur/setup-php@v2
      with:
        php-version: '8.5'
        extensions: ctype, iconv, intl, pdo_pgsql
        tools: composer:v2
        coverage: none

    - name: Get Composer cache directory
      id: composer-cache
      shell: bash
      working-directory: RecipeHub
      run: echo "dir=$(composer config cache-files-dir)" >> "$GITHUB_OUTPUT"

    - name: Cache Composer dependencies
      uses: actions/cache@v4
      with:
        path: ${{ steps.composer-cache.outputs.dir }}
        key: ${{ runner.os }}-composer-${{ hashFiles('RecipeHub/composer.lock') }}
        restore-keys: ${{ runner.os }}-composer-

    - name: Install Composer dependencies
      shell: bash
      working-directory: RecipeHub
      run: composer install --prefer-dist --no-progress --no-interaction

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '22'
        cache: npm
        cache-dependency-path: RecipeHub/package-lock.json

    - name: Install npm dependencies
      shell: bash
      working-directory: RecipeHub
      run: npm ci
