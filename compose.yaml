services:
  app:
    image: ${RECIPEHUB_IMAGE:-recipehub-app:latest}
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      APP_ENV: prod
      DATABASE_URL: "postgresql://recipehub:recipehub_password@postgres:5432/recipehub?serverVersion=16&charset=utf8"
      LOG_DIR: /var/log/recipehub
      LOG_LEVEL: info
    ports:
      - "8080:8080"
    volumes:
      - recipehub_logs:/var/log/recipehub

  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: recipehub
      POSTGRES_USER: recipehub
      POSTGRES_PASSWORD: recipehub_password
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U recipehub -d recipehub"]
      interval: 5s
      timeout: 5s
      retries: 5
      start_period: 10s
    volumes:
      - postgres_data:/var/lib/postgresql/data

  loki:
    image: grafana/loki:2.9.8
    command: -config.file=/etc/loki/loki-config.yaml
    volumes:
      - ./docker/loki/loki-config.yaml:/etc/loki/loki-config.yaml:ro
      - loki_data:/loki

  promtail:
    image: grafana/promtail:2.9.8
    command: -config.file=/etc/promtail/promtail-config.yaml
    depends_on:
      - loki
    volumes:
      - ./docker/promtail/promtail-config.yaml:/etc/promtail/promtail-config.yaml:ro
      - recipehub_logs:/var/log/recipehub:ro

  grafana:
    image: grafana/grafana:11.5.2
    depends_on:
      - loki
    environment:
      GF_SECURITY_ADMIN_USER: admin
      GF_SECURITY_ADMIN_PASSWORD: admin
    ports:
      - "3000:3000"
    volumes:
      - grafana_data:/var/lib/grafana
      - ./docker/grafana/provisioning/datasources/loki.yaml:/etc/grafana/provisioning/datasources/loki.yaml:ro

volumes:
  postgres_data:
  recipehub_logs:
  loki_data:
  grafana_data:
